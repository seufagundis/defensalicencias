<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>Contacto - Defensa Licencias AMBA</title>

    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>

    <link
      href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;700;800&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
      rel="stylesheet"
    />

    <script id="tailwind-config">
      tailwind.config = {
        darkMode: "class",
        theme: {
          extend: {
            colors: {
              primary: "#0f1829",
              "accent-yellow": "#facc15",
              "background-light": "#f6f7f8",
              "background-dark": "#14181e",
            },
            fontFamily: {
              display: ["Manrope", "sans-serif"],
            },
            borderRadius: {
              DEFAULT: "0.25rem",
              lg: "0.5rem",
              xl: "0.75rem",
              full: "9999px",
            },
          },
        },
      };
    </script>

    <style>
      .material-symbols-outlined {
        font-variation-settings: "FILL" 0, "wght" 400, "GRAD" 0, "opsz" 24;
      }
      html, body {
        font-family: Manrope, ui-sans-serif, system-ui;
      }
    </style>
  </head>

  <body class="bg-background-light text-primary antialiased">
    <!-- Header -->
    <%- include("partials/header") %>

    <main class="max-w-5xl mx-auto px-6 md:px-10 lg:px-40 py-16">
      <div class="max-w-3xl">
        <h1 class="text-4xl md:text-5xl font-black tracking-tight mb-4">
          Contacto
        </h1>
        <p class="text-primary/70 text-lg mb-10">
          Canal único de atención: WhatsApp.
        </p>
      </div>

      <section class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <!-- Card principal -->
        <div class="lg:col-span-2 bg-white border border-black/10 rounded-2xl p-8 md:p-10 shadow-sm">
          <h2 class="text-2xl font-extrabold mb-3">Iniciar conversación</h2>
          <p class="text-primary/70 mb-8">
            Atendemos AMBA (CABA + Conurbano). Te respondemos dentro del horario de atención.
          </p>

          <div class="flex flex-col sm:flex-row gap-3">
            <!-- CTA principal (amarillo) -->
            <a
              href="<%= whatsappLink %>"
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center justify-center bg-accent-yellow text-primary px-7 py-4 rounded-xl font-extrabold hover:opacity-90 transition-all"
            >
              Iniciar por WhatsApp
            </a>

            <!-- CTA secundario (oscuro) -->
            <a
              href="/faq"
              class="inline-flex items-center justify-center bg-primary text-white px-7 py-4 rounded-xl font-bold hover:opacity-90 transition-all"
            >
              Ver preguntas frecuentes
            </a>
          </div>

          <p class="text-sm text-primary/60 mt-6">
            Información general. La viabilidad depende del caso.
          </p>
        </div>

        <!-- Card lateral -->
        <aside class="bg-white border border-black/10 rounded-2xl p-8 shadow-sm">
          <h3 class="text-lg font-extrabold mb-4">Antes de escribir</h3>

          <ul class="space-y-3 text-primary/70 text-sm">
            <li class="flex gap-2">
              <span class="material-symbols-outlined text-primary text-lg">check_circle</span>
              Decinos si es CABA o Provincia (AMBA).
            </li>
            <li class="flex gap-2">
              <span class="material-symbols-outlined text-primary text-lg">check_circle</span>
              Indicá tipo de licencia (particular / profesional).
            </li>
            <li class="flex gap-2">
              <span class="material-symbols-outlined text-primary text-lg">check_circle</span>
              Contanos si hay multas o deuda aproximada.
            </li>
          </ul>

          <div class="mt-6 border-t border-black/10 pt-6">
            <p class="text-sm text-primary/60">
              Horario de atención: <strong><%= process.env.HORARIO_ATENCION || "ver footer" %></strong>
            </p>
          </div>
        </aside>
      </section>
    </main>

    <%- include("partials/footer") %>

    <!-- Floating WhatsApp -->
    <%- include("partials/floating-whatsapp") %>
  </body>
</html>
